[project]
name = "agentops-cloud"
version = "0.1.0"
description = "AgentOps Cloud SDK — automatic agent discovery, telemetry, heartbeat, cost and tool tracking. Not yet implemented; see docs/Roadmap.md (Phase 2)."
requires-python = ">=3.9"
readme = "README.md"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["agentops_cloud"]
